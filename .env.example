# Variáveis lidas pelo docker-compose.yml (domínio público + e-mail de contato do certbot).
# Copie para .env e edite. O backend continua lendo ./backend/.env separadamente.
DOMAIN=backup.seudominio.com.br
CERTBOT_EMAIL=admin@seudominio.com.br
